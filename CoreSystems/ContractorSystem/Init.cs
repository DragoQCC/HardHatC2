using HardHatCore.ContractorSystem.Contexts;

namespace HardHatCore.ContractorSystem
{
    public class Init
    {
        public async Task Initialize()
        {
            foreach (var invocationStartItem in InvocationData.InvocationStartItems)
            {

            }
            foreach (var invocationEndItem in InvocationData.InvocationEndItems)
            {

            }
        }

    }
}
